[build]
builder = "DOCKERFILE"
dockerfilePath = "showup-api/Dockerfile"

[deploy]
startCommand = "./showup-api"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "showup-api"
port = 8080
